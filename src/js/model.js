export const state = {
  task: {},
};

let category = 'Category from JS';
let taskName = 'Task from JS';
let dueDate = 'Nov 1';
let completed = false;

state.task = {
  category: category,
  taskName: taskName,
  dueDate: dueDate,
  completed: false,
};
